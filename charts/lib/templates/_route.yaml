{{- define "lib.route" -}}
{{- range .Values.routes -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $.Release.Name }}-{{ .name }}
  labels:
    runs-on: {{ .runsOn }}
    instance: {{ $.Release.Name }}-{{ .name }}
    component: {{ .component | default "httpRoute" }}
    part-of: {{ $.Release.Name }}
spec:
  parentRefs:
    - name: {{ $.Values.env }}-gateway
      kind: Gateway
  hostnames:
    - {{ .host }}
  rules:
     - matches:
        - path:
            type: PathPrefix
            value: /
       backendRefs:
        - name: {{ $.Release.Name }}-{{ .runsOn }}
          port: {{ .port | default 80 }}
---
{{- end -}}
{{- end -}}